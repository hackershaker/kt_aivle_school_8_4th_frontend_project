version: 0.2

cache:
  paths:
    - node_modules/**/*
phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  base-directory: dist
  files:
    - '**/*'